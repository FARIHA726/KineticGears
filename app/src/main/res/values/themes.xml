<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Kineticgear2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- RED COLORS -->
        <item name="colorPrimary">@color/red_primary</item>
        <item name="colorPrimaryVariant">@color/red_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- BEIGE COLORS -->
        <item name="colorSecondary">@color/beige_dark</item>
        <item name="android:windowBackground">@color/beige_light</item>

        <!-- STATUS BAR -->
        <item name="android:statusBarColor">@color/red_dark</item>
    </style>
</resources>